import UIKit

var defaults = UserDefaults.standard //this is an example of singleton.
var urls = URLSession.shared // this is an example of singleton.
//one copy of it that can be shared across all of your classes and objects. 
